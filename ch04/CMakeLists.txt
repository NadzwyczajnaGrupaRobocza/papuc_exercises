
add_subdirectory(lm)
add_subdirectory(mb)
add_subdirectory(ib)

# include_directories(${COMMON_INCLUDES})

# add_executable(ch04-tests ../gtest_main.cc lm/ex02.cpp)

# target_link_libraries(ch04-tests ch04-lm)
# target_link_libraries(ch04-tests gtest)
